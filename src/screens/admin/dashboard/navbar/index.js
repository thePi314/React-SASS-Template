import React from "react";

const AdminDashboardNavbar = ({ className }) => {
    return (
        <div className={`navbar ${className ?? ''}`}>
            
        </div>
    )
}

export default AdminDashboardNavbar;
